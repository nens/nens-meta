# Initially generated by nens-meta

[build-system]
requires = ["setuptools>=69"] # Suggested by nens-meta

[project]
name = "example-project" # Set by nens-meta
requires-python = ">=3.11" # Suggested by nens-meta
dependencies = [] # Suggested by nens-meta
description = "I really need to set this" # Suggested by nens-meta
authors = [] # Suggested by nens-meta
readme = "README.md" # Suggested by nens-meta

[project.optional-dependencies]
test = ["pytest"] # pytest added by nens-meta

[tool.setuptools]
packages = ["example_project"] # Set by nens-meta
zip-safe = false

[tool.pytest.ini_options]
testpaths = ["example_project"] # Set by nens-meta
log_level = "DEBUG" # Suggested by nens-meta

[tool.coverage.run]
source = ["example_project"] # Set by nens-meta

[tool.coverage.report] # show_missing and skip_empty set by nens-meta
show_missing = true
skip_empty = true

[tool.ruff]
target-version = "py38" # Suggested by nens-meta

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"] # Suggested by nens-meta, please add 'UP' and 'C901'

[tool.zest-releaser] # Suggested by nens-meta, adjust from setup.cfg if needed
release = false

[tool.pyright]
include = ["example_project"] # Set by nens-meta
venvPath = "." # Set by nens-meta
venv = ".venv" # Set by nens-meta
